(function(){
  

  document.write( '<%= stylesheet_link_tag embed_link_url(@user, @gist, "css") %>' );
  document.write( '<%= j(render @gist).html_safe %>' );

  var 
    base  = document.getElementById( 'jw-<%= @gist.url %>' ),
    body  = base.getElementsByClassName( 'gist-body' )[0],
    image = body.getElementsByClassName( 'jw_image' )[0],
    lineNumbers = body.getElementsByClassName( 'line-numbers' )[0];
  
  lineNumbers.style.height = image.height + 'px';
  body.style.height        = image.height + 'px';

}).call( this );